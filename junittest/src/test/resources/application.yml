server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/junittest?serverTimezone=UTC&characterEncoding=utf-8&allowMultiQueries=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.junit.junittest.db
